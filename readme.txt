The R file(fds_gmeans.R) contains gmeans(X=hw5.3d.data,significance_level=5) function.


the function accepts the following values of significance levels(in percentage) "10", "5", "2.5", "1", "0.5","0.1","0.05","0.01". the function will take the dataset named "hw5.3d.data" in your environment or you can specify the dataframe. 


The function generates plots during each iteration THE PATHS TO WHICH HAVE TO BE CHANGED to store it on your desktop. 

they are sections of code that generate plots. they have been marked as follows
#***************

lines of code 

#**************






HClust.r contains other clustering techniques applied on the data.



 